-- Anti-AFK Script with Stylish GUI
-- Requires an Executor (e.g., Synapse X, Delta, Solara)

-- 1. Create the Main GUI Frame
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AntiAfkGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35) -- Dark stylish background
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -150, 0.1, 0)
MainFrame.Size = UDim2.new(0, 300, 0, 250)
MainFrame.BackgroundTransparency = 0.1

-- Add a corner for style
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

-- 2. Title Label
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
Title.Text = "Anti-AFK Tool"
Title.TextColor3 = Color3.fromRGB(0, 255, 200) -- Cyan text
Title.TextSize = 20
Title.Font = Enum.Font.GothamBold
Title.Size = UDim2.new(1, 0, 0, 50)
Title.TextXAlignment = Enum.TextXAlignment.Center
Title.BorderSizePixel = 0

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = Title

-- 3. Status Label
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Name = "StatusLabel"
StatusLabel.Parent = MainFrame
StatusLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
StatusLabel.Text = "Status: Disabled"
StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
StatusLabel.TextSize = 14
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.Size = UDim2.new(0.8, 0, 0, 30)
StatusLabel.Position = UDim2.new(0.1, 0, 0.2, 0)
StatusLabel.BorderSizePixel = 0

-- 4. Toggle Button
local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = MainFrame
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 150, 100) -- Green when off, will change
ToggleButton.Text = "ENABLE"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 16
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Size = UDim2.new(0.6, 0, 0, 40)
ToggleButton.Position = UDim2.new(0.2, 0, 0.35, 0)
ToggleButton.BorderSizePixel = 0

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 5)
ButtonCorner.Parent = ToggleButton

-- 5. Checkboxes for Keys
local CheckBoxW = Instance.new("TextButton")
CheckBoxW.Name = "CheckBoxW"
CheckBoxW.Parent = MainFrame
CheckBoxW.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
CheckBoxW.Text = "Press W"
CheckBoxW.TextColor3 = Color3.fromRGB(255, 255, 255)
CheckBoxW.TextSize = 14
CheckBoxW.Font = Enum.Font.Gotham
CheckBoxW.Size = UDim2.new(0.35, 0, 0, 30)
CheckBoxW.Position = UDim2.new(0.05, 0, 0.55, 0)
CheckBoxW.BorderSizePixel = 0

local CheckBoxWCorner = Instance.new("UICorner")
CheckBoxWCorner.CornerRadius = UDim.new(0, 5)
CheckBoxWCorner.Parent = CheckBoxW

local CheckBoxD = Instance.new("TextButton")
CheckBoxD.Name = "CheckBoxD"
CheckBoxD.Parent = MainFrame
CheckBoxD.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
CheckBoxD.Text = "Press D"
CheckBoxD.TextColor3 = Color3.fromRGB(255, 255, 255)
CheckBoxD.TextSize = 14
CheckBoxD.Font = Enum.Font.Gotham
CheckBoxD.Size = UDim2.new(0.35, 0, 0, 30)
CheckBoxD.Position = UDim2.new(0.45, 0, 0.55, 0)
CheckBoxD.BorderSizePixel = 0

local CheckBoxDCorner = Instance.new("UICorner")
CheckBoxDCorner.CornerRadius = UDim.new(0, 5)
CheckBoxDCorner.Parent = CheckBoxD

local CheckBoxMouse = Instance.new("TextButton")
CheckBoxMouse.Name = "CheckBoxMouse"
CheckBoxMouse.Parent = MainFrame
CheckBoxMouse.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
CheckBoxMouse.Text = "Click LMB"
CheckBoxMouse.TextColor3 = Color3.fromRGB(255, 255, 255)
CheckBoxMouse.TextSize = 14
CheckBoxMouse.Font = Enum.Font.Gotham
CheckBoxMouse.Size = UDim2.new(0.35, 0, 0, 30)
CheckBoxMouse.Position = UDim2.new(0.85, -140, 0.55, 0) -- Adjusted position
CheckBoxMouse.BorderSizePixel = 0

local CheckBoxMouseCorner = Instance.new("UICorner")
CheckBoxMouseCorner.CornerRadius = UDim.new(0, 5)
CheckBoxMouseCorner.Parent = CheckBoxMouse

-- 6. Draggable Functionality
local UserInputService = game:GetService("UserInputService")
local DragStart
local Dragging = false
local DragInput
local DragSpeed = 0.3

MainFrame.InputBegan:Connect(function(Input)
	if Input.UserInputType == Enum.UserInputType.MouseButton1 then
		Dragging = true
		DragStart = Input.Position
		MainFrame.Active = true
	end
end)

MainFrame.InputChanged:Connect(function(Input)
	if Input.UserInputType == Enum.UserInputType.MouseMovement then
		DragInput = Input
	end
end)

UserInputService.InputChanged:Connect(function(Input)
	if Input == DragInput and Dragging then
		local delta = Input.Position - DragStart
		MainFrame.Position = UDim2.new(
			MainFrame.Position.X.Scale,
			MainFrame.Position.X.Offset + delta.X * DragSpeed,
			MainFrame.Position.Y.Scale,
			MainFrame.Position.Y.Offset + delta.Y * DragSpeed
		)
	end
end)

UserInputService.InputEnded:Connect(function(Input)
	if Input.UserInputType == Enum.UserInputType.MouseButton1 then
		Dragging = false
	end
end)

-- 7. Logic for Anti-AFK
local VirtualUser = game:GetService("VirtualUser")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local isRunning = false
local interval = 60 -- 1 minute in seconds

-- Helper to simulate key press
local function simulateKeyPress(key)
	if key == "W" then
		VirtualUser:CaptureController()
		VirtualUser:ClickButton2(Vector2.new()) -- Simulates key press via button 2 (often used for generic input) or specific key if supported
		-- Note: VirtualUser doesn't have a direct 'PressKey' for WASD in all executors. 
		-- A common workaround is to use :ClickButton2() which simulates a right-click, 
		-- but for WASD, some executors support :ChaseMouse() or similar. 
		-- However, the most reliable way for WASD in many exploits is to use 
	   
